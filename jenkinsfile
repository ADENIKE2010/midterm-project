pipeline {
  agent any
  stages {
    stage('Ansible Deployment') {
      steps {
        script {
          ansiblePlaybook(
            playbook: 'midterm.yml',
            inventory: 'hosts.ini'
          )
        }
      }
    }
  }
}
